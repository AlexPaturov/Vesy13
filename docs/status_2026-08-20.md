# Статус проекта Vesy13

Актуализировано: 2026-08-20

## Репозиторий

- Проект: `/home/lex/shared/Vesy13`.
- Основная ветка: `master`.
- Актуальный последний коммит проверять командой `git log -1 --oneline`.
- Перед началом работы проверять `git status --short`.
- Для восстановления контекста в новой сессии читать этот файл и `git log --oneline -n 10`.
- Работать экономно по выводу: точечные `rg`/`sed`, без больших diff в консоль без запроса.

## Приложение

- WinForms-приложение на C#; основной проект сейчас на `net6.0-windows7.0`.
- Основные формы:
  - `MainForm` — главное меню;
  - `StaticWeighingForm` — статическое потележечное взвешивание;
  - `DynamicWeighingForm` — динамическое потележечное взвешивание;
  - `ServiceForm` — сервисные режимы, калибровка и настройки;
  - `CorrectionsForm` — корректировка и перенос данных;
  - `PrintForm` — выборка и печать отвесной;
  - `LogsForm` — просмотр журнала аудита;
  - `PasswordDialog` — ввод и смена административного доступа.
- Общие стили форм живут в `UiColors` и `UiFonts`.
- Сервисная форма использует отдельные `ServiceUiColors` и `ServiceUiFonts`.
- Пользовательская инструкция: `docs/user/user-manual.md`.
- Отчёты для руководителя: `docs/boss_reports/`.

## Настройки и установка

- Рабочие настройки хранятся в общем каталоге данных приложения.
- При первом запуске учитывается перенос ранее сохранённых настроек.
- Пароль администратора хранится как PBKDF2 hash + salt.
- Локальный кэш калибровки хранится в настройках и используется при недоступности базы на старте.
- Установка программы и установка локальной базы являются независимыми пакетами.
- Каталог данных приложения должен быть доступен оператору на запись.

## База данных

- Локальная база: PostgreSQL `scale_db`.
- Подключение приложения к локальной базе выполняется локально без пароля для пользователя приложения.
- Таблица аудита создаётся приложением при необходимости.
- Основные таблицы описаны в `docs/database.md`.
- Для статической калибровки таблица `calibration_points` содержит обязательное поле `calibration_value INTEGER NOT NULL`.
- Среди активных статических точек одного канала уникальны масса и код АЦП.
- Установщик пересоздаёт локальную БД по итоговому `scale_db.sql`; миграции схемы не используются.

## Аудит и ошибки

- Аудит различает успешные действия, отклонённые операции и ошибки.
- Технические тексты исключений не выводятся оператору в пользовательских сообщениях.
- Исключения пишутся в локальную базу.
- Если локальная база недоступна, исключение пишется в файл fallback-журнала.
- Внутренние ошибки самого механизма аудита не запускают повторную запись через тот же механизм.
- В журнал пишутся открытия и закрытия форм.
- В журнал пишутся изменения состояния связи с АЦП на формах взвешивания и сервисной форме.

## АЦП и связь

- АЦП возвращает оба канала: CH0 и CH1.
- Выбор канала означает выбор используемого значения из кадра.
- COM-порт: 4800 бод, Even parity, 8 бит, 1 стоп-бит.
- Статика:
  - запрос poll byte `214`;
  - ответ 4 байта;
  - `CH0 = byte[1] * 256 + byte[0]`;
  - `CH1 = byte[3] * 256 + byte[2]`.
- Динамика:
  - установка потока `Set=126`, `Req=254`;
  - поток 5-байтовых сэмплов;
  - `CH0_LO CH0_HI CH1_LO CH1_HI AUX`;
  - `AUX` хранится как диагностический байт.
- Формы используют собственные reader-ы, чтобы вкладки и режимы не держали общий COM-порт.
- При переключении вкладок сервисной формы конфликтующие reader-ы закрываются.

## Взвешивание

- Вагоны взвешиваются потележечно.
- После второй тележки программа суммирует массу вагона и сохраняет результат.
- `TrainTime` фиксируется при первой тележке первого вагона состава.
- `WagonTime` фиксируется при второй тележке каждого вагона.
- Нумерация вагонов идёт внутри состава и сбрасывается завершением состава.
- Дискретность применяется к отображаемой и сохраняемой массе.
- Превышение НПВ подсвечивается и блокирует перенос в Firebird.
- На динамической форме после сохранения вагона показатели тележек сбрасываются.

## Статическая калибровка

- Статическая калибровка хранится набором точек по каналам.
- Каждая сохранённая точка неизменяема: код АЦП, масса и калибровочное число не редактируются задним числом.
- Изменение калибровки выполняется добавлением новой точки; существующую точку при необходимости делают неактивной.
- Неактивная точка остаётся в истории через `deleted_at`.
- Активные точки одного канала не могут иметь одинаковую массу или код АЦП.
- Расчёт веса использует сохранённое калибровочное число:
  `вес = код АЦП * calibration_value / 65535`.
- В гриде статики колонка калибровочного числа называется `Калибр. число`.
- Чекбокс `_chbCalibCounter` управляет режимом расчёта:
  - отмечен — автоматический расчёт отключён, значение вводится вручную;
  - снят — значение рассчитывается по коду АЦП и массе для новых строк.
- При ручном режиме панель статической калибровки выделяется красным цветом.
- В аудит статической калибровки пишутся все поля изменённой точки, включая `calibration_value`.
- Диагностика статического взвешивания пишет `calibrationValue` выбранной точки.

## Поправочные коэффициенты направления

- Поправочные коэффициенты направления хранится историческими строками в `direction_correction_profiles`.
- Активная строка выбирается по `is_active = TRUE` и пустому `deleted_at`.
- `RightDirectionCorrectionFactor = 0` и `LeftDirectionCorrectionFactor = 0` являются валидными значениями.
- Новое сохранение поправочных коэффициентов направления снимает прежнюю активную строку и добавляет новую.
- Коэффициенты динамики сохраняются и пишутся в аудит с округлением до 5 знаков.
- В аудит динамики пишутся поля прежнего профиля, сделанного неактивным, и добавленного нового профиля.
- Динамическое взвешивание допускается только при активной статической точке
  текущего канала, активном профиле и положительном коэффициенте выбранного
  направления.
- Формула динамического веса: `статический вес * поправочный коэффициент
  направления`; статический вес рассчитывается по выбранной статической точке.
- Нулевые значения коэффициентов можно хранить в профиле, но ими нельзя
  выполнять динамическое взвешивание.

## Связь результата с калибровкой

- `wagon_weighing` хранит nullable-ссылки `bogie1_calibration_point_id`,
  `bogie2_calibration_point_id` и `direction_correction_profile_id`.
- Статический результат сохраняет две ссылки на статические точки; динамический
  — две ссылки на точки и ссылку на профиль направления.
- Внешние ключи используют `ON DELETE RESTRICT`; результаты можно очищать без
  удаления исторических точек и профилей.
- Очистка внутри приложения пока не реализована. Ежемесячное задание с
  `psql.exe` будет исключено: после применения white list запуск `psql.exe`
  станет невозможен. Детали новой реализации очистки согласуются отдельно.

## Фильтры входного потока

- Фильтры находятся между reader-ом и формой.
- Сырые события и состояние связи приходят напрямую от reader-а.
- Отбракованный кадр не публикуется в форму.
- Параметры фильтров хранятся в настройках приложения.
- Описание поведения фильтров: `docs/hardware.md`.
- Описание параметров: `docs/configuration.md`.

## Логи интерфейса

- Живые высокочастотные логи используют owner-drawn `ListBox`.
- Для сервисных логов действует ограничение числа строк.
- RichTextBox не используется как живой лог высокой частоты.

## Проверки и ограничения окружения

- На текущем Linux-хосте полноценная сборка WinForms-проекта не проходит из-за отсутствующего `Microsoft.NET.Sdk.WindowsDesktop`.
- Компиляция и проверка WinForms выполняются на Windows/Rider.
- Для локальной ревизии использовать:
  - `git status --short`;
  - `git diff --stat`;
  - `git diff --check` по затронутым не-дизайнерским файлам;
  - точечный поиск `rg` по изменённым сущностям.
- `ServiceForm.Designer.cs` может содержать CRLF/trailing whitespace после WinForms Designer; содержательные изменения проверять отдельно.
- Не откатывать чужие незакоммиченные изменения без прямого запроса.

## Conventional Commits

Используем стандарт + добавили **`breaking:`** как тип верхнего уровня.

Типы:

```
feat       — новая функциональность
fix        — исправление бага
breaking   — ломающее изменение (видно в git log сразу!)
chore      — обслуживание
docs       — документация
refactor   — рефакторинг
test       — тесты
build      — сборка, Docker, CI
ci         — GitHub Actions
style      — форматирование
perf       — производительность
```
