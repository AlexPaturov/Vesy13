# Статус работы за 2026-06-12

## Текущее состояние репозитория
- Рабочая ветка: `master`.
- `master` синхронизирован с `origin/master`.
- Ветка `serviceform-designer-refactor` влита в `master` и запушена.
- Merge-коммит: `d893091 merge serviceform-designer-refactor`.
- Tracked-файлы после push чистые.
- Untracked остаются вне коммитов:
  - `MainForm.resx`
  - `ScaleListener/MainForm.resx`

## Основные выполненные работы
- Исправлена форма статики:
  - убрана строка `Готов к взвешиванию`;
  - добавлены отдельные индикаторы веса 1-й и 2-й тележки;
  - основной индикатор веса продолжает обновляться после фиксации 1-й тележки;
  - после успешного сохранения вагона индикаторы тележек очищаются.
- В `SimA04Reader` добавлена очистка входного и выходного буфера COM-порта при открытии.
- Зафиксированы и реализованы локальные настройки через `settings.json` рядом с `.exe`:
  - автосоздание файла при отсутствии;
  - COM-порт по умолчанию `COM1`;
  - НПВ `140 т`;
  - дискретность `0.05 т`;
  - лимит нуля весовщика `2% НПВ`;
  - админ-лимит нуля `100% НПВ`;
  - пароль администратора хранится как `PBKDF2 hash + salt`.
- `MainForm` теперь берет рабочий COM-порт из `settings.json`.
- `ServiceForm -> Мониторинг` оставлен без изменения как ручной диагностический режим.
- `ServiceForm -> Настройки` сохраняет COM-порт, НПВ, дискретность, лимит нуля и новый пароль.
- Удалены нереализуемые пользовательские настройки:
  - таймаут тележек;
  - окно динамики как пользовательская настройка.
- Смена активного канала CH0/CH1 оставлена доступной весовщику как аварийное переключение и логируется.
- Дискретность применяется к отображаемым и сохраняемым значениям массы.
- Кнопка `Ноль` подключена к лимиту из настроек.
- В `CorrectionsForm` добавлена проверка превышения НПВ:
  - строки сверх НПВ подсвечиваются красным фоном и белым текстом;
  - перенос в Firebird блокируется;
  - при попытке переноса показывается предупреждение и очищаются поля корректировки/доввода.
- Обновлены документы:
  - `docs/notes.md`;
  - `docs/rules.md`;
  - `docs/plan.md`;
  - `docs/тз13.md`.

## Сегодняшние коммиты
- `25497f8 fix: restore pending grid focus after transfer`
- `009f072 docs: update corrections form notes`
- `816c6aa fix: center logs form filters and limit size`
- `45b5f6e feat: export logs to csv`
- `f649616 feat: soft delete calibration points`
- `a51489c fix: use calibration point coefficient for static weight`
- `0621b31 feat: manage adc lifecycle and simulator`
- `345b41f fix: improve static weighing indicators`
- `503f1e5 fix: clear adc port buffers on open`
- `6d61edf chore: remove unsupported bogie timeout setting`
- `2ee5702 feat: add local app settings`
- `d893091 merge serviceform-designer-refactor`

## Проверки
- `git diff --check` проходил перед коммитами.
- `dotnet build Vesy13.csproj` в текущем Linux-окружении не проходит из-за отсутствия `Microsoft.NET.Sdk.WindowsDesktop`; до компиляции кода MSBuild не доходит.

## Что важно помнить дальше
- `settings.json` создается рядом с `.exe`, не должен коммититься как пользовательский файл конфигурации.
- Для полноценной проверки сборки нужен Windows/.NET SDK с `Microsoft.NET.Sdk.WindowsDesktop`.
- Перед следующим коммитом проверить, нужны ли untracked `MainForm.resx` и `ScaleListener/MainForm.resx`; сегодня они намеренно не включались.
